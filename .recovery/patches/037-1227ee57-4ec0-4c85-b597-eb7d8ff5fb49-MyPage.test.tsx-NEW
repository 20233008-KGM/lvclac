    expect(html).toContain(`>${en.myPage.setPasswordAction}</button>`)
    // Google-only 사용자는 이메일 수단이 미연동
    expect(html).toContain('my-page-linked-status is-unlinked')
    expect(html).not.toContain(`${en.myPage.providerLinked} · ${en.myPage.primaryTag}`)