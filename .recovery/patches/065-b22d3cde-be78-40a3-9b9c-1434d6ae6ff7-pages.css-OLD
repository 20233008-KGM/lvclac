.my-page-actions {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-sm);
  margin-top: var(--space-md);
}

/* 구독 결제 패널 */
.my-page-billing {
  display: grid;
  gap: var(--space-sm);
  margin-top: var(--space-sm);
}